// This file was auto-generated by SourceMod (v1.12.0.7217)
// ConVars for plugin "gg2_bot_smoke_suppress.smx"


// Clamp a seeded bot's arousal to this value so it stays under the engine's IsMinArousal(8) gate, which routes aroused bots to cover instead of suppressing. 0 = off. Without this the mechanic works for roughly 30 seconds and then stops permanently.
// -
// Default: "6.5"
// Minimum: "0.000000"
// Maximum: "10.000000"
sm_bot_smoke_suppress_arousal_cap "6.5"

// Per-pass chance that an eligible bot is given knowledge of a smoked player. 0.0-1.0.
// -
// Default: "0.25"
// Minimum: "0.000000"
// Maximum: "1.000000"
sm_bot_smoke_suppress_chance "0.25"

// Comma-separated substrings matched against entity classnames to identify smoke clouds.
// -
// Default: "grenade_m18,grenade_smoke"
sm_bot_smoke_suppress_classnames "grenade_m18,grenade_smoke"

// Log every suppression decision. Noisy - troubleshooting only.
// -
// Default: "0"
sm_bot_smoke_suppress_debug "0"

// Let bots blind-fire at smoke they have not seen anyone enter. 0 = off (default).
// -
// Default: "0"
sm_bot_smoke_suppress_enabled "1"

// Chance a seeded bot entering the attack branch is steered onto the grenade-throw action.
// -
// Default: "0.0"
// Minimum: "0.000000"
// Maximum: "1.000000"
sm_bot_smoke_suppress_grenade_chance "0.0"

// Weapon class to report at the attack dispatch. 2=frag 3=molotov 4=smoke 7=AT4.
// -
// Default: "2"
sm_bot_smoke_suppress_grenade_class "2"

// Let bots standing inside the smoke cloud suppress as well, by clearing their blinded flag.
// -
// Default: "0"
sm_bot_smoke_suppress_include_in_smoke "0"

// Seconds between passes. Each pass rolls the chance once per eligible bot.
// -
// Default: "2.0"
// Minimum: "0.500000"
// Maximum: "30.000000"
sm_bot_smoke_suppress_interval "2.0"

// Seconds after seeding during which bots are denied pursuit of that player, so they shoot instead of walking in. Pair with ins_bot_suppressing_fire_duration.
// -
// Default: "6.0"
// Minimum: "0.000000"
// Maximum: "60.000000"
sm_bot_smoke_suppress_nopursue_time "6.0"

// How close a player must be to a smoke cloud to be considered hidden by it.
// -
// Default: "250.0"
// Minimum: "0.000000"
sm_bot_smoke_suppress_radius "250.0"

// Maximum distance from a bot to the smoke for that bot to be eligible.
// -
// Default: "1500.0"
// Minimum: "0.000000"
sm_bot_smoke_suppress_range "1500.0"

// Top up a seeded bot's RESERVE ammo when it falls near the engine's 10% suppression cutoff. Without this the mechanic self-limits to roughly one burst per bot life, because suppressing fire empties a magazine the bot would not otherwise have spent.
// -
// Default: "1"
// Minimum: "0.000000"
// Maximum: "1.000000"
sm_bot_smoke_suppress_refill_ammo "1"

// Forget the injected threat before re-adding it, so the bot treats each seed as a NEW acquisition. Without this, re-adding an already-known entity does not move the behavior back into the Combat action and bots engage the first smoke only.
// -
// Default: "1"
// Minimum: "0.000000"
// Maximum: "1.000000"
sm_bot_smoke_suppress_refresh_threat "1"

// Seconds before the same bot may be seeded again. Lets the injected threat age past the engine's suppression threshold.
// -
// Default: "8.0"
// Minimum: "0.000000"
// Maximum: "60.000000"
sm_bot_smoke_suppress_reseed_cooldown "8.0"

// Seconds a smoke stays tracked. Should roughly match how long the cloud is actually visible.
// -
// Default: "18.0"
// Minimum: "1.000000"
// Maximum: "120.000000"
sm_bot_smoke_suppress_smoke_life "18.0"

// Seconds a bot may hold an unthrown grenade before being switched back to a firearm (0 = never). A bot holding a grenade fails the engine's weapon-class check and cannot suppress meanwhile. Default 0: bots were observed putting the grenade away on their o
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "30.000000"
sm_bot_smoke_suppress_unstick_grenade "0"

// Warn players on screen the first few times they are near smoke that bots may fire into it. Only ever fires while the mechanic itself is enabled.
// -
// Default: "1"
// Minimum: "0.000000"
// Maximum: "1.000000"
sm_bot_smoke_suppress_warn "1"

// Warning colour as "R G B A".
// -
// Default: "255 200 60 255"
sm_bot_smoke_suppress_warn_color "255 200 60 255"

// A player whose last warning was longer ago than this is treated as new and starts the count again. 0 = never forget.
// -
// Default: "90"
// Minimum: "0.000000"
sm_bot_smoke_suppress_warn_forget_days "90"

// Seconds the warning stays on screen.
// -
// Default: "10.0"
// Minimum: "0.500000"
sm_bot_smoke_suppress_warn_hold "10.0"

// How many times a player is ever shown the warning. 0 = unlimited.
// -
// Default: "10"
// Minimum: "0.000000"
sm_bot_smoke_suppress_warn_max "10"

// How close to a live cloud a player must be to be warned. Distance rather than line of sight - it is one check per player per sweep either way, and being beside a cloud you cannot see is exactly when the mechanic surprises people.
// -
// Default: "1500.0"
// Minimum: "0.000000"
sm_bot_smoke_suppress_warn_radius "1500.0"

// The warning text. Keep it short - it is a popup, not a briefing.
// -
// Default: "Enemies may fire blindly into smoke on this server."
sm_bot_smoke_suppress_warn_text "Enemies may fire blindly into smoke on this server."

// Horizontal position, 0.0-1.0 across the screen. -1 centres it.
// -
// Default: "-1"
sm_bot_smoke_suppress_warn_x "-1"

// Vertical position, 0.0-1.0 down the screen. -1 centres it.
// -
// Default: "0.65"
sm_bot_smoke_suppress_warn_y "0.65"

// Weapon classes allowed to suppress. 9=SMG 10=rifle 12=LMG are native; anything else here is promoted so the engine accepts it (e.g. add 11 for DMRs).
// -
// Default: "9,10,12"
sm_bot_smoke_suppress_weapon_classes "9,10,12"



// Added by hand. AutoExecConfig only writes this file when it is missing, so convars added to the
// plugin after it was first generated never appear here on their own - they silently run on their
// code defaults instead. Stated explicitly so the surface is a deliberate choice and tunable.

// Warning surface. 0 = game_text overlay, 1 = Game Instructor world hint on the cloud, 2 = hint text.
// 1 is the tutorial-style world callout. It obeys the client's own gameinstructor_enable, which the
// server cannot override - use 2 if that turns out to matter.
// -
// Default: "1"
sm_bot_smoke_suppress_warn_style "1"

// Game Instructor icon for the warning (style 1 only). Names come from
// scripts/instructor_textures.txt: icon_caution, icon_alert, icon_alert_red, icon_bulb.
// -
// Default: "icon_caution"
sm_bot_smoke_suppress_warn_icon "icon_caution"
